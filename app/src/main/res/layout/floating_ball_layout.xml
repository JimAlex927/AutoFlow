<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/floating_ball_touch_target"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:padding="0dp">

    <ImageView
        android:id="@+id/ball_dock_handle_left"
        android:layout_width="24dp"
        android:layout_height="42dp"
        android:layout_gravity="center"
        android:background="@drawable/floating_ball_dock_handle_left"
        android:contentDescription="展开悬浮球"
        android:gravity="center"
        android:padding="5dp"
        android:src="@drawable/ic_dock_chevron_right"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/floating_ball_core"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/floating_ball_container"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/floating_ball_modern_bg"
            android:clickable="false"
            android:elevation="6dp"
            android:focusable="false">

            <ImageView
                android:id="@+id/floatingBall"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:contentDescription="floating ball"
                android:src="@drawable/logo_seven_manifold_foreground" />
        </FrameLayout>

        <TextView
            android:id="@+id/ball_status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="7dp"
            android:background="@drawable/floating_ball_status_bg"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:maxWidth="148dp"
            android:minHeight="24dp"
            android:paddingStart="9dp"
            android:paddingTop="4dp"
            android:paddingEnd="9dp"
            android:paddingBottom="4dp"
            android:text=""
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </LinearLayout>

    <ImageView
        android:id="@+id/ball_dock_handle_right"
        android:layout_width="24dp"
        android:layout_height="42dp"
        android:layout_gravity="center"
        android:background="@drawable/floating_ball_dock_handle_right"
        android:contentDescription="展开悬浮球"
        android:gravity="center"
        android:padding="5dp"
        android:src="@drawable/ic_dock_chevron_left"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/ball_dock_handle_bottom"
        android:layout_width="42dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:background="@drawable/floating_ball_dock_handle_bottom"
        android:contentDescription="展开悬浮球"
        android:gravity="center"
        android:padding="5dp"
        android:src="@drawable/ic_dock_chevron_up"
        android:visibility="gone" />

</FrameLayout>
