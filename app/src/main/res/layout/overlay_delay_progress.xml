<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="176dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="10dp"
    android:paddingTop="8dp"
    android:paddingEnd="10dp"
    android:paddingBottom="8dp"
    android:background="@drawable/overlay_delay_progress_bg">

    <TextView
        android:id="@+id/tv_delay_overlay_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="延迟 0s / 0s"
        android:textColor="#FFF1F1"
        android:textSize="11sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <ProgressBar
        android:id="@+id/progress_delay_overlay"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginTop="6dp"
        android:indeterminate="false"
        android:max="1000"
        android:progress="0"
        android:progressDrawable="@drawable/overlay_delay_progress_drawable" />

</LinearLayout>
